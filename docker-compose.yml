# Local “run the hub like prod” (optional). MCP still uses Cursor, not this compose.
services:
  meeting-hub:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      PORT: "8000"
